<Window x:Class="TipaDiplom.Views.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="👨‍🏫 Панель преподавателя" 
        Height="750" Width="1100"
        WindowStartupLocation="CenterScreen"
        Background="#1E1E2E">

    <Window.Resources>
        <Style x:Key="AdminMenuBtn" TargetType="Button">
            <Setter Property="Background" Value="#313244"/>
            <Setter Property="Foreground" Value="#CDD6F4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment=
                                "{TemplateBinding HorizontalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45475A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Боковая панель -->
        <Border Grid.Column="0" Background="#181825">
            <DockPanel>
                <!-- Кнопка выхода -->
                <Button DockPanel.Dock="Bottom" Content="🚪 Выйти"
                        Margin="10,10" Padding="10,8" FontSize="13"
                        Background="#F38BA8" Foreground="#1E1E2E"
                        BorderThickness="0" Cursor="Hand"
                        FontWeight="SemiBold"
                        Click="Logout_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <StackPanel Margin="10,20,10,10">
                    <!-- Заголовок -->
                    <TextBlock Text="👨‍🏫" FontSize="40"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="Преподаватель" FontSize="18"
                               FontWeight="Bold" Foreground="#89B4FA"
                               HorizontalAlignment="Center" Margin="0,5,0,5"/>
                    <TextBlock Text="admin" FontSize="13"
                               Foreground="#6C7086"
                               HorizontalAlignment="Center" Margin="0,0,0,20"/>

                    <Separator Background="#45475A" Margin="0,0,0,10"/>

                    <TextBlock Text="МЕНЮ" Foreground="#6C7086"
                               FontSize="11" FontWeight="Bold" Margin="5,0,0,8"/>

                    <Button Content="📊 Результаты студентов"
                            Style="{StaticResource AdminMenuBtn}"
                            Click="ShowResults_Click"/>
                    <Button Content="👥 Список студентов"
                            Style="{StaticResource AdminMenuBtn}"
                            Click="ShowStudents_Click"/>
                    <Button Content="📚 Управление теорией"
                            Style="{StaticResource AdminMenuBtn}"
                            Click="ShowTheory_Click"/>
                    <Button Content="✏️ Управление тестами"
                            Style="{StaticResource AdminMenuBtn}"
                            Click="ShowTests_Click"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Основной контент -->
        <Border Grid.Column="1" Margin="10" 
                Background="#181825" CornerRadius="12">
            <Frame x:Name="AdminFrame" NavigationUIVisibility="Hidden"/>
        </Border>
    </Grid>
</Window>